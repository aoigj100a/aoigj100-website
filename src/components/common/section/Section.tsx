export const Section = ({ children }) => {
    return (
        <section className="px-[8px] border-sky-900 border-b-[2px] lg:px-[160px] pb-[120px]">
            {children}
        </section>
    );
};
